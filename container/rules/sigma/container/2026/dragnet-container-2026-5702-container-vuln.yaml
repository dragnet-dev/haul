title: 'Dragnet [Container]: dragnet-container-2026-5702 — Vulnerable Base Image Pull'
id: 25086066-0dc6-5ef4-882c-b0037cd1d133
status: deprecated
description: |
  Detects deployment of a container image known to contain a critical vulnerability.
  Incident: dragnet-container-2026-5702 (Tier 2, CVSS 9.1)
  A possible security vulnerability has been identified in Apache Kafka.

By default, the broker property `sasl.oauthbearer.jwt.validator.class` is set to `org.apache.kafka.common.security.oauthbearer.DefaultJwtValidator`. It accepts any JWT token without validating its signature, issuer, or audience. An attacker can generate a JWT token from any issuer with the `preferred_username` set to any user, and the broker will accept it.

We advise the Kafka users using kafka v4.1.0 or v4.1.1 to set the config `sasl.oauthbearer.jwt.validator.class` to `org.apache.kafka.common.security.oauthbearer.BrokerJwtValidator` explicitly to avoid this vulnerability. Since Kafka v4.1.2 and v4.2.0 and later, the issue is fixed and will correctly validate the JWT token.
references:
  - http://www.openwall.com/lists/oss-security/2026/04/17/2
  - https://access.redhat.com/security/cve/CVE-2026-33557
  - https://github.com/apache/kafka
  - https://github.com/apache/kafka/commit/01d8e7db8d08dbd538892b409457ea6bfcc2a422
  - https://kafka.apache.org/cve-list
  - https://lists.apache.org/thread/v57o00hm6yszdpdnvqx2ss4561yh953h
  - https://nvd.nist.gov/vuln/detail/CVE-2026-33557
  - https://www.cve.org/CVERecord?id=CVE-2026-33557
author: dragnet-bot
date: 2026-05-17
tags:
  - attack.t1610
  - attack.t1611
  - dragnet.cve
logsource:
  category: container_image_pull
detection:
  selection:
    Image|contains:
      - 'chainguard:'
      - 'wolfi:'
  condition: selection
falsepositives:
  - Cached images pulled before the vulnerability was published
  - Internal image mirrors that have not been updated
level: high
