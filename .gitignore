# Trivy vulnerability DB — binary, >1 GB, rebuilt from cache on each run
state/trivy_cache/

# OSSF malicious-packages clone — separate git repo, managed via Actions cache
state/ossf-cache/

# dragnet binary downloaded by CI
dragnet
